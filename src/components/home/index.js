/*
 * 模块组件库
 * 这里放置各个模块的组件，如home、shop、login等
 * 但各个模块应创建各自的文件夹，方便维护
 */

import HelloWorld from './hello-world'
import BusSend from './bus-demo-send'
import BusReceive from './bus-demo-receive'

export {
    HelloWorld,
    BusSend,
    BusReceive
}
